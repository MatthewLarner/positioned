{
  "name": "position",
  "version": "1.0.0",
  "description": "Callback when an element has a position",
  "main": "index.js",
  "scripts": {
    "test": "browserify ./tests -o ./tests/index.browser.js && google-chrome ./tests/index.html",
    "watch": "watchify ./tests -o ./tests/index.browser.js & google-chrome ./tests/index.html"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "laidout": "^1.0.0"
  },
  "devDependencies": {
    "browserify": "^11.1.0",
    "crel": "^2.1.8",
    "tape": "^4.2.0"
  }
}
